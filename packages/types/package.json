{
  "name": "@vaultkeeper/types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "@vaultkeeper/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
